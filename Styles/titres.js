import { StyleSheet } from "react-native"

export const titres = StyleSheet.create({
    principal: {
        fontSize: 30,
        fontWeight: 'bold'
    },
    secondaire: {
        fontSize: 25
    }
})